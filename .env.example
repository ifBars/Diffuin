PORT=8787
GITHUB_APP_ID=123456
# Local/Docker Compose: mount the PEM and use GITHUB_PRIVATE_KEY_PATH.
GITHUB_PRIVATE_KEY_PATH=/run/secrets/github-app-private-key.pem
# Hosted platforms: omit the path and set the PEM as base64 in this secret instead.
# GITHUB_PRIVATE_KEY_BASE64=
GITHUB_WEBHOOK_SECRET=replace-with-a-long-random-secret
DIFFUIN_HANDLE=Diffuin
ALLOWED_REPOSITORIES=octo-org/example-repo
DATA_DIR=/data
CODEX_MODEL=gpt-5.6-luna
CODEX_ALLOWED_MODELS=gpt-5.6-luna,gpt-5.6-terra,gpt-5.6-sol,gpt-5.3-codex-spark
CODEX_REASONING_EFFORT=max
CODEX_REASONING_ROUTING=true
SPARK_COMMAND=spark
SPARK_MODELS=gpt-5.3-codex-spark
SPARK_TIMEOUT_MS=1800000
# Keep false unless cmd.exec is separately confined by the host/container.
SPARK_ALLOW_UNSANDBOXED_COMMANDS=false
# SPARK_CMD_EXEC_DOCKER_CONTAINER=diffuin-spark-sandbox
# SPARK_CMD_EXEC_DOCKER_WORKDIR=/workspace
JOB_POLL_INTERVAL_MS=1000
SCHEDULE_ONE_SKILL_PATH=/app/skills/schedule-one-modding
SCHEDULE_ONE_CODE_ARCHIVER_URL=https://github.com/k073l/s1-codearchiver.git
# Optional private compact corpus. Keep it out of Git and the container image.
# SCHEDULE_ONE_ASSETRIPPER_PATH=/data/references/assetripper
